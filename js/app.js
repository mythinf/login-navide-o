// cargando todo el árbol de nodos del DOM
//  carga asi : login, interactive(parseo)-complete( se conecta con recursos externos:tdn,..)

$(document).ready(function() {
  // variable constante
  //  var limite = 140;
  // Accediendo al primer P
  //  var paragraph = $('p').first();
  //  var paragraph = $('p').eq(0);

  //  console.log(paragraph[0]);
  //  console.log(paragraph.eq(0));
  //  console.log(paragraph[0]);
  setTimeout(function() {
    window.location.href = 'views/register.html';
  }, 3000);
});
